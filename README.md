
  # Arts Undergraduate Society Website
  AUS website redesign - fall 2025

  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # aus-redesign
